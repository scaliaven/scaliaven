# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a GitHub profile repository. Its sole content is `README.md`, which renders on the GitHub profile page for [scaliaven](https://github.com/scaliaven). There is no build system, test suite, or application code.

## README conventions

- Uses raw HTML (`<h1>`, `<p align>`, `<img>`) alongside Markdown for layout control, since GitHub's profile README renderer supports it.
- Tool icons are sourced from the `devicons` CDN and rendered as linked images.
- GitHub Stats widgets are pulled from `github-readme-stats.vercel.app`.
- Keep the personal voice — the "young and dangerous" framing is intentional.
